
version: '3.8'

services:
  mongodb:
    image: mongo:8
    restart: always
    container_name: mongodb
    ports:
      - 27017:27017
    networks:
      - mongo-network
    command: ["--replSet", "dbrs", "--bind_ip_all"]

networks:
  mongo-network:
    driver: bridge